let score = "33"
//we can set any data type in simple let call
console.log(typeof score)
let intNumber = Number(score)// converting into number
console.log(intNumber);
console.log(typeof intNumber);
 // what if the string is some thing like this 33abs? then also it will be convert it into the number with NAN value
 // if null-> so 0 if true then 1 false then 0 if  string with character then NAN.
